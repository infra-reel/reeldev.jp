# ⚠️ このファイルはテンプレートです。
# 実際の値は kubectl create secret で投入するか、
# Sealed Secrets / External Secrets Operator を使用してください。
# ─────────────────────────────────────────────────────────────
# kubectl create secret generic reeldev-secrets -n reeldev \
#   --from-literal=ADMIN_API_KEY='<strong-random-key>' \
#   --from-literal=SESSION_SECRET='<strong-random-key>' \
#   --from-literal=DISCORD_CLIENT_ID='<discord-app-client-id>' \
#   --from-literal=DISCORD_CLIENT_SECRET='<discord-app-client-secret>' \
#   --from-literal=DISCORD_REDIRECT_URI='https://admin.reeldev.jp/auth/callback' \
#   --from-literal=ALLOWED_DISCORD_IDS='<your-discord-user-id>' \
#   --from-literal=QIITA_USER='riel_hosiduki' \
#   --from-literal=QIITA_TOKEN='<optional-qiita-token>' \
#   --from-literal=API_BASE='http://api-service:3001' \
#   --from-literal=CORS_ORIGIN='https://reeldev.jp'
---
# GHCR image pull secret
# kubectl create secret docker-registry ghcr-secret -n reeldev \
#   --docker-server=ghcr.io \
#   --docker-username=<github-username> \
#   --docker-password=<github-pat>  # scopes: read:packages
